# AGENTS.md

## Repository

- The actual game is inline in `index.html`; a real `Game` engine (rAF loop, state machine, input poll, pooling hooks, flow field, spatial hash) already exists there — there is no separate clean game file to retrofit.
- **Data split (2026-09-04):** the pure-data registries (STAT_DEF/Data/CHARS/WEAPONS/ENEMIES/ARENAS/ACHIEVEMENTS) moved out of the engine script into `data.js`, loaded by `<script src="data.js">` immediately before the engine `<script>`. Both are classic scripts so the top-level `const`s share the global lexical environment — `data.js` MUST stay ahead of the engine script (gate marker guards it) and next to `index.html` (relative include, file://-safe). Tables still inline: BOSSES/ITEMS/DANGERS/MODS. `data.js` ends with a guarded `module.exports` for Node require. If index.html ever again contains `const CHARS = [` inline while data.js exists, the double declaration breaks the load — the gate's FORBIDDEN list flags that. sw.js `SHELL`/CACHE stamp are manual here (`tools/build.js` stamper referenced in its comment does not exist in this workspace) — re-stamp CACHE from the sha1 of index.html whenever the shell changes.
- First-run key safety: keep the optional `seedInput` on `scTitle` from stealing focus on load; the title screen is the most likely place for cold-start key hijacking.
- Audio lifecycle is gesture-gated: `Game.init()` must not create an `AudioContext`; `AudioSys.init()` is allowed through the intro/UI gesture or `startRun()`. A suspended context is still a cold-start side effect for QA.
- Endscreen QA is not natural-play proof: wave 20 is not reachable in a bounded live run, so direct `Game.endRun(true|false)` or `?qa` controls must be labeled white-box; repeated terminal calls are expected to be idempotent for saves and presentation cues.

## Tooling / environment

- This harness may not have puppeteer or reliable background/static serving; full interactive validation can fall back to a briefly served copy plus a browser probe or structural inspection.
- The browser inspection tool may need a served copy rather than a raw `file://` URL to expose load/console state clearly.
- **Video toolchain IS available (verified 2026-09-09; the old "no usable video toolchain" note was stale).** `ffmpeg`/`ffprobe`/`ffplay` 9.0.1-full_build (gyan.dev) are on the user PATH at `%LOCALAPPDATA%\Microsoft\WinGet\Packages\Gyan.FFmpeg_*\ffmpeg-9.0.1-full_build\bin`. Exercised end to end here: encode (`testsrc2`+`sine` → 1280x720@60 H.264/AAC, probed 180 frames / 3.000 s), `ffprobe` inspection, `drawtext` overlay (rendered frame visually confirmed), PNG-sequence → MP4, MP4 → GIF transcode, `-f gdigrab -i desktop` screen capture, and `h264_nvenc` hardware encode. Encoders present: libx264/libx265/libsvtav1/libvpx-vp9/libaom/libwebp/libmp3lame/aac/gif; also libass, libfreetype, libvmaf, libplacebo, nvenc/amf/qsv/vaapi. So MP4s CAN be inspected, labeled, overlaid, and re-encoded here — no hand-off-only excuse for video deliverables.
  - Windows gotchas that look like "ffmpeg is broken" but are not: (1) **fontconfig is unconfigured** — `drawtext=font=Arial` dies with `Fontconfig error: Cannot load default config file` and an access violation (exit `-1073741819`); always pass an explicit `fontfile=`. (2) The **drive colon in `fontfile=C:/...` breaks filter parsing** (`No option name near '/Windows/...'`) and `C\:` is eaten by argv unescaping; the reliable fixes are to copy the .ttf next to the media and use a bare relative `fontfile=arial.ttf` (Push-Location into the workdir), or put the filter in a file and pass `-/filter:v <file>`. (3) `-filter_script:v` was **removed** in ffmpeg 8/9 — it errors `Unrecognized option`; the replacement is the `-/filter:v` read-from-file prefix. (4) `Remove-Item "$dir\*"` on a `%TEMP%` scratch dir is blocked by the harness sandbox as a protected path — make a fresh `ffmpeg-e2e-$(Get-Random)` dir instead of clearing one. (5) `$env:VAR` does not survive between PowerShell tool calls (only the cwd does) — re-derive scratch paths each call.
- The audio toolchain is LOCAL and ffmpeg-only, no ElevenLabs needed for the shipped pipeline: `tools/synth-audio.mjs` generates 21 AAC game SFX into `audio/` (manifest-pinned; data-regression 3j asserts manifest ⊆ BAKE_SLOT and files present), `tools/trailer-audio.mjs` synthesizes the 20 s BGM bed plus German TTS VO via Microsoft Hedda (de-DE, System.Speech) into `trailer/media/`, and `tools/trailer-build.mjs` mixes bed+VO with deterministic window ducking. `AudioSys.prefetchAssets()` (index.html) loads `audio/*.m4a` ONLY over http(s) after the audio-init gesture; file:// silently keeps the offline bake. The gesture gate is pinned twice (2026-09-10): the SelfTest `_audioGestureGate` group (?selftest) fails on ANY `audio/*.m4a` fetch attempt before the first **trusted** (`isTrusted`) gesture — synthetic suite pokes are deliberately not gestures, so the group blocks `AudioSys.init` for the suite window and politely blocks asset fetches (200 + junk bytes, real error path, no wedge) — and `prefetchAssets` itself refuses to run while `!started` (second lock at the loader, so a future direct caller cannot bypass the gate). `ELEVENLABS_API_KEY` stays unneeded for this pipeline; licensed assets would still be the next quality step.
- The browser preview bridge can throw page-context errors even when a preview is registered; if in-page assertions stop cooperating, treat that as 'cannot fully verify over the bridge right now' and fall back to a clean served copy, a headless probe, or careful structural reads — do not pretend the bridge confirmed behavior it did not.
- Spawning a static server by background Python/process from this harness is unreliable; if a real-surface playtest needs HTTP(S), start the server through a locally reliable method first, then register the preview — do not assume a helper-launched server is still alive between turns.
- Real-browser path that works here: the `agent-browser` CLI is installed, but its managed browser cache and system Chrome are empty; Windows Edge ships at `C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe`. Launch it headless with `--remote-debugging-port` + a fresh scratch `--user-data-dir` (+ `--disable-extensions --no-first-run`), then `agent-browser connect <port>`; rAF fires there, so real-time play is verifiable without engine-stepping. Background process spawn is unsupported — launch detached via `powershell Start-Process`. Prefer the packaged gate over hand-rolling this: `node tools/verify.mjs` serves the workspace, drives `?selftest` in headless Edge (asserts SELFTEST n/n · PASS with n ≥ 100 — the count grows as assertions are added) plus a second `?selftest&qa=1` pass proving the positive QA-Nav branch (row shown, all buttons ring members, 131/131 vs the plain 134/134 — count-agnostic banner, branch-switch proof by result names). The selftest's codex groups now also pin the screen-reader contract (`SelfTest._codexNavGate` ring vs `SelfTest._codexA11y` reading order): content rows are keyboard-excluded (`data-navskip`) but MUST stay AT-reachable — `#codexList` is `role=list`, every row `role=listitem` + `aria-label`, nothing aria-hidden — so an exclusion implemented as aria-hidden/display:none trips the suite. The suite itself now ends with an async Balance-Parity group (`SelfTest._balanceParity`: computeChunked(2000) must match compute(2000) exactly; `_asyncPending` defers the banner render until it lands, so a poll can never accept the sync-only state). The selftest also ships one generic gated-row contract (`SelfTest._gatedRowHidden`/`_gatedRowShown`) registered by simRow and qaRow (hidden + unnavable on plain load, visible under their flag), the inverse player-surface contract (`_surfaceContracts`: scCode/codePanel and the Feedback row must STAY visible+navigable — they're features, not dev surfaces), and a controls seed-copy contract (`_controlsSeedCopy`: the seed row must truthfully show `Tages-Seed · AZ <n>` under a daily run — with the Neuer-Seed reroll button absent, since `startRun()` ignores `manualSeed` there — `Zufall` otherwise, the manual number when typed). The gate drives Edge through ONE raw-CDP WebSocket bound to the page target — no agent-browser daemon (evals routed via the daemon flapped on stale targets from killed Edge sessions). Runs a 10k BalanceSim sync + chunked leg (yields, bounded slices, parity) + a throttled 4x/6x leg (Emulation.setCPUThrottlingRate: engagement via calibration ratio, worst slice < SLICE_MS budget + one tail unit, no SUSTAINED rAF freeze — outliers classified by a parallel setTimeout(0) ticker) and checks the CE_VALID flag — ~15 s, exits non-zero on failure; `--no-browser` for static legs only. Headless launch needs the `--disable-background-timer-throttling` family of flags or chained `setTimeout(0)` (async probes, chunked sim) crawls at ~1 Hz — same trap as rAF throttling. Fast-start cheat sheet with launch snippets + the drift/cache pitfalls: `docs/edge-cdp-playtest-handoff.md`. Gotchas encoded there: Node cannot spawn the extension-less `agent-browser` shim (resolve the real exe under `%APPDATA%\npm\node_modules\agent-browser\bin`), the raw exe cold-starts its daemon only when spawned detached with ignored stdio (pre-warm `--version` first), PowerShell needs `-NoProfile -Command` as separate argv with single-quote-only scripts, and a fresh Edge profile can open `edge://sync-confirmation-dialog/` — activate your tab via `tab list`/`tab <n>` and launch with `--disable-features=msEdgeFirstRunExperience`.
- Edge pitfalls: a stale Edge instance locks its profile dir (before deleting it, kill only msedge processes whose command line contains that profile path) and can silently keep a drifted tab active — always check `location.href` in eval output; the long-running “seedHelp missing at runtime” false alarm was a wrong-tab artifact, not a page bug.
- The preview never fires `requestAnimationFrame` (engine time frozen even with state `play`/focused). Advance runs with bounded `Game.update(1/60)` batches — legitimate, since the page’s own `?selftest` LongRun drives `update()` directly. Wall-clock counters (Spielzeit) show 0:00 under stepping: a surface artifact, not a game defect.
- Native `confirm()` blocks automation and looks like a hang — stub `window.confirm=()=>true` first. The preview can serve a cached old page: navigate with `?cb=N` and verify an edit marker in the live DOM before trusting reads.
- A11y snapshots uppercase text that the DOM holds mixed-case (CSS `text-transform`): prefer `data-act`/id handles and case-insensitive matches; option toggles and offer cards are clickable divs, not `<button>`s.
- If `F:` is not mounted, .NET/PowerShell recursive enumeration reports an unavailable scope rather than an empty one; preserve that distinction in `docs/f-audio-search.md` and never claim a full-drive result. Check drive existence before spending time on recursive scans.

## Git state (this workspace)

- `index.html` was flagged `assume-unchanged` (CE_VALID): `git ls-files -v index.html` returned lowercase `h`, so every working-tree edit to it was invisible to status/diff/commit — a clobber hazard in this shared checkout. Now cleared (`H`); if it regresses, fix with `git update-index --no-assume-unchanged index.html`.
- **Who set the flag (2026-09-03 forensics):** the Freebuff workspace materializer, one-time at workspace creation. Evidence: workspace lives in `C:\freebuff\wbn_massaka` (harness-owned, single squashed synthetic commit by `tu.blitzbert`, no remote, sample-only hooks, no aliases/fsmonitor, no active hooks); the flag was applied while `index.html` was byte-pristine (index-cached size 5,327,000 == HEAD blob exactly), so it predates all session edits; `dev/ino=0` is normal Windows git, not a tool marker; no `update-index` in bash/PowerShell history (not the user); no IDE configs; the desktop app bundle contains no such logic. Only `index.html` was flagged because it is the one file a workspace sync keeps authoritative. It was NOT re-applied during this session after clearing (survived dozens of git ops + patch applies).
- **Real hazard is not the flag but the sync:** the snapshot blob (6b6a69a, 5,327,000 B) no longer exists anywhere on disk; a harness re-materialization / sync refresh that copies the master `index.html` over the working copy would silently clobber the session's change set. On startup, tripwire: run `node tools/verify.mjs` (fails if the flag is re-applied, if the session's change-set markers vanish from the working file, if the data.js split regresses — markers missing, tables back inline, data.js declarations lost — or if the served copy diverges from it); `git ls-files -v index.html` must print uppercase `H`, and `git diff --stat index.html` should still show the session's hunks (do NOT trust memorized numbers — they rot on every commit: 2026-09-03 it was ~+195/−740 vs the pristine blob, 2026-09-10 ~+45/−1 vs HEAD; re-derive each time and compare `git hash-object index.html` against the last known working state); if `index.html` suddenly looks untouched, do NOT trust it — compare `git hash-object index.html` against the recorded working state.
- Repo history is one squashed commit; session artifacts live untracked in `docs/`, `skills/`, `tools/`, `trailer/`.

## Scope rule of thumb

- Prefer documenting “what good looks like” (first-run acceptance items) over large rewrites when the engine already works; the current structural ceiling is one large embedded game+UI page, so later changes remain more brittle than in a separated spine.
- Verification/playtest passes report with a first-line `GRADES spec= design= correctness= quality=; biggest gap:` and grade harshly: correctness only from behavior actually exercised (unexercised caps at 6), design ≤5 while the app stays one/two files; leave a dated per-pass record in `docs/`.
